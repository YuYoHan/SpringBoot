spring:
  datasource:
    url: //localhost:3306/web0135
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

mybatis:
  mapper-locations: mybatis/mapper/**/*.xml
  type-aliases-package: com.example.board2.mapper
